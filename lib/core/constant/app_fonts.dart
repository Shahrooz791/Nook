class AppFonts {
  AppFonts._();

  /// Primary UI font — used for almost everything (buttons, labels, body,
  /// headings that aren't letter/journal content).
  static const String poppins = 'Poppins';

  /// Serif accent font — used only for letter content and a few warm,
  /// personal headings (e.g. "To: Future Me", letter titles).
  static const String serif = 'CormorantGaramond';
}
